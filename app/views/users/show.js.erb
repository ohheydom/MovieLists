$('nav.pagination').html("<%= escape_javascript (paginate @usermoviesp, remote: true)%>");
$('.endless_movie').append("<%= escape_javascript (render :partial => 'users/partials/list_of_movies', :collection => @usermoviesp, :as => :movie)  %>");
$('#tempload').remove();
