$('nav.pagination').html("<%= escape_javascript (paginate @paginated_list, remote: true)%>");
$('.endless_movie').append("<%= escape_javascript (render partial: @listpart, collection: @paginated_list, as: :movie)  %>");
$('#tempload').remove();
