$('nav.pagination').html("<%= escape_javascript (paginate @listp, remote: true)%>");
$('.endless_movie').append("<%= escape_javascript (render :partial => @listpart, :collection => @listp, :as => :movie)  %>");
$('#tempload').remove();
